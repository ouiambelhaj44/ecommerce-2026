{% extends 'base.html.twig' %}

{% block title %}Products - Our Store{% endblock %}

{% block body %}
    <div class="container my-5">
        <h1 class="mb-4">Our Products</h1>
        
        <div class="row row-cols-1 row-cols-md-3 g-4">
            <div class="col">
                <div class="card h-100">
                    {# T-akad bli item.png raha f public/image/ #}
                    <img src="{{ asset('image/item.png') }}" class="card-img-top" alt="Product 1">
                    <div class="card-body">
                        <span class="badge bg-primary mb-2">Electronics</span>
                        <h5 class="card-title">Wireless Headphones</h5>
                        <p class="card-text fw-bold text-success">$79.99</p>
                        <a href="#" class="btn btn-outline-primary">View Details</a>
                    </div>
                </div>
            </div>
            
            {# ... (kopi l-products l-o khrin hna) ... #}
        </div>
    </div>
{% endblock %}